cat > README.md << 'EOF'
# Terraform - Развертывание MySQL базы данных в VK Cloud

Этот проект автоматизирует развертывание инстанса MySQL 8.0 в конфигурации Single Instance на платформе VK Cloud с заполнением тестовыми данными.

## 🚀 Возможности

- **MySQL 8.0 Single Instance** с использованием Terraform
- **Диск 20GB SSD** с авторасширением до 1000GB
- **Внешний IP-адрес** для удаленного доступа
- **Автоматическое заполнение** тестовыми данными (200 таблиц × 50 столбцов × 10 записей)

## 🏗️ Архитектура

- **СУБД**: MySQL 8.0 Single Instance
- **Конфигурация**: STD2-2-8 (2 vCPU, 8GB RAM)
- **Хранилище**: 20GB Ceph-SSD
- **Доступ**: Внутренний + внешний IP

## ⚡ Быстрый старт

### 1. Развертывание инфраструктуры:
```
terraform init
terraform apply -var="username=ваш_email" -var="password=ваш_пароль" -var="project_id=ваш_project_id"
```
### 2. Заполнение тестовыми данными:

```
python3 populate_database.py \\
  $(terraform output -raw mysql_host_internal) \\
  "app_user" \\
  "7h78gs.p70aG85wU0" \\
  "app_database"
```
📁 Структура проекта
main.tf - Основная конфигурация Terraform

variables.tf - Переменные конфигурации

provider.tf - Настройка провайдера VK Cloud

populate_database.py - Скрипт генерации тестовых данных

.gitignore - Игнорируемые файлы

📋 Требования
Python 3.x с mysql-connector-python

🎯 Результаты развертывания
Хост БД: 89.208.208.28

База данных: app_database

Пользователь: app_user

Таблицы: 200 тестовых таблиц с данными

🔧 Тестовые данные
После выполнения скрипта заполнения создаются:

200 таблиц с именами test_table_001 ... test_table_200

50 столбцов в каждой таблице

10 записей в каждой таблице

Случайные данные длиной 4-8 символов в каждом столбце

Всего значений: 200 × 10 × 50 = 100,000

Пароли управляются через переменные Terraform
